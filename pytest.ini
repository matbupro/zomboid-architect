[pytest]
asyncio_mode = auto
markers =
    e2e: tests d'integration avec vrais appels Steam/steamcmd (lents, requis reseau)
    slow: tests couteux en temps (scan large, benchmark)
testpaths = tests
pythonpath = .
