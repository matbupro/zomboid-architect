-- Auto-generated by Zomboid Architect
-- Mod    : {{ mod_name }}
-- Script : {{ script_name }} (shared)

----------------------------------------------------------------------
-- Shared definitions — charge sur both client et serveur
----------------------------------------------------------------------

local MOD_NAME = "{{ mod_name }}"
local MOD_SCRIPT_DIR = "scripts/{{ script_dir }}"

{% if features_json and features_json != "[]" %}
-- Feature constants (passees par le generateur) :
local CONSTANTS = {{ features_json | trim }}
{% endif %}

-- Export functions accessible depuis d'autres scripts
function MOD_NAME.GetScriptDir()
    return MOD_SCRIPT_DIR
end

-- Hook PZ standard
Hooks.Register("OnGameInit", function()
    print(string.format("[MOD %s] Shared script loaded", MOD_NAME))
end)
