-- Auto-generated by Zomboid Architect
-- Mod    : {{ mod_name }}
-- Script : {{ script_name }} (server-side)

----------------------------------------------------------------------
-- Server-side hook registration
----------------------------------------------------------------------

Hooks.Register("OnGameInit", function()
    --[[
    Initialisation server-side.
    Ex: IsoSpawner.SpawnItem(item_id, x, y, z), etc.
    ]]
end)

{% if features_json and features_json != "[]" %}
-- Feature definitions (passed by generator) :
local FEATURE_DEFS = {{ features_json | trim }}

for _, feat in ipairs(FEATURE_DEFS) do
    -- Exemple: spawn items on the server
    if feat.type == "spawn_item" then
        IsoSpawner.SpawnItem(feat.item_id, feat.x or 0, feat.y or 0, feat.z or 0)
    end
end
{% endif %}
