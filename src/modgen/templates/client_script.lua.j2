-- Auto-generated by Zomboid Architect
-- Mod    : {{ mod_name }}
-- Script : {{ script_name }} (client-side)

----------------------------------------------------------------------
-- Client-side hook registration
----------------------------------------------------------------------

Hooks.Register("OnGameInit", function()
    --[[
    Initialisation client-side.
    Utiliser les variables de jeu via la classe PZ 'Client' ou 'Events'.
    ]]
end)

{% if features_json and features_json != "[]" %}
-- Features du mod (passees par le generateur) :
local FEATURE_DEFS = {{ features_json | trim }}

for _, feat in ipairs(FEATURE_DEFS) do
    if feat.type == "add_item" then
        InventoryItemFactory.AddItemOrSpawn(feat.item_id, feat.quantity or 1)
    end
end
{% endif %}
