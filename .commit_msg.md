feat(storage): S9 — PostgreSQL uniquement (suppression SQLite)

Suppression de SQLite du runtime : le backend PG/pgvector est maintenant le seul.

DELETE:
  src/storage/sqlite_storage.py       (1234 lignes)
  tests/test_sqlite_storage.py        (436 lignes)
  tests/test_dual_backend.py          (380 lignes)
  migrations/convert_sqlite_to_pg.py  (~439 lignes)

REFACTOR:
  src/storage/__init__.py → create_backend() factory PG-only
  PostgresStorageBackend.backend_type ajouté (property manquante)
  ingestor/generate_report.py._collect_collections() infos= [] fix (UnboundLocalError)

MIGRATION callers (20+ fichiers):
  Tous imports sqlite_storage → src.storage (create_backend)
  bot/engine_client.py, src/retrieval/__init__.py, ingestor/storage/storage_writer.py
  ingestor/generate_report.py, ingestor/config.py
  tests/test_*.py (5 fichiers adaptés)

CONFIG:
  STORAGE_BACKEND default → postgres partout (ingestor/config.py + .env templates)
  STORAGE_DUAL_SYNC supprimé (legacy)

DOCS:
  ARCHITECTURE.md — diagramme PG/pgvector uniquement, suppr "SQLite Backend"
  SETUP.md — PG natif Windows instructions (winget), default=postgres
  README.md — storage section clean, STORAGE_BACKEND=postgres
  ingestor/README.md — "SQLite vectoriel" → "PostgreSQL/pgvector"
  bot/README.md — STORAGE_BACKEND default + architecture PG
  Makefile — STORAGE_BACKEND=postgres
  CHANGELOG.md — entry v0.4.1-alpha S9 migration

VALIDATION:
  Postgres backend tests: 19/21 pass (1 skip asyncpg absent, 1 mock pre-existing)
  Import check: create_backend() → PostgresStorageBackend OK